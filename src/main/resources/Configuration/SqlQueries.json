{
  "//": "The select queries to retrieve data from the Test Data tables in the database",
  "selectqueries": {
    "HRMLoginTestData": "select * from hrm_testdata;"
  },
  "//": "The runner list queries to retrieve data from the Runner List table in the database",
  "runnerlistqueries": {
    "HRMRunnerlist": "select * from hrm_runmanager;"
  }
}
