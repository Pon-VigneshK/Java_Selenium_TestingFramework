package org.gcit.enums;

/**
 * The LogType enum represents various levels of logging severity.
 * It is used to categorize the nature of the log messages.
 */
public enum LogType {
    FATAL, TRACE, WARN, DEBUG, ERROR, INFO;
}
